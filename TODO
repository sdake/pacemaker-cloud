TODO list
=========

a == Angus
s == Steven
u == Unassigned

CPE
---
u5) extend upstart/systemd client code to include getting events on failure.
u6) at startup use config_list() to get initial deployables

DPE
---
u7) generate QMF events for changes in state for:
    deployables (running, failed)
u4) Add support for failure escalations

TESTS
-----
a4) Convert tests to use the shell

Common lib
----------
u2) add API to access deployable config from exteranl API (cloud specific managemnt DB)
        config_get(deployment_uuid)
        config_list()

SHELL
-----
Add the following commands:
u1) deployable_status
    show assembly online/offline
    show resource status
a1) receive dped notifications
s1) launching of assembly via vm launcher so we can do stop/start of vm
s2) restart of vm/application on vm after failure detected
u7) add setup.py to install python scripts
